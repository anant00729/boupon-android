<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".main.fragments.SettingsFragment">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/tv_title"
        android:textColor="@android:color/black"
        android:textSize="@dimen/sixteen"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="@string/settings"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/sixty_four" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_show_settings_list"
        android:layout_below="@id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>


    <Button
        android:id="@+id/btn_logout"
        android:layout_marginTop="20dp"
        android:layout_below="@id/rv_show_settings_list"
        android:layout_centerHorizontal="true"
        android:text="@string/logout"
        android:textColor="@drawable/btn_select_text_color"
        android:background="@drawable/btn_select"
        android:layout_width="wrap_content"
        android:layout_height="40dp" />

</RelativeLayout>
